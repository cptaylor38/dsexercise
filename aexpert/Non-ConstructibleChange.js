function nonConstructibleChange(coins) {
    coins.sort();
    //sort, then find a missing number, check numbers before missing number to see if they total the missing number
    //if not
    //choose one number above the last number in the array.

    //mapping out logic
  }

//1, 3, 4, 5, 6, 7, 8, 9 - Expected result 2.
nonConstructibleChange([1, 2, 4, 5])